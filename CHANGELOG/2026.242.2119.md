# 2026.242.2119 — chore(selkies): advance stale pins so published tags carry current refs

## Summary

**3 of this candy's 4 pinned refs** were behind their repos' newest tags. That matters beyond
tidiness: a published tag embeds the pins it carried at tag time, so every consumer pinning a
tag of this candy drags those old refs into its own resolution, and charly reports them as
`resolved to multiple versions` on every build of that consumer.

The candy's own CalVer moves too (`YYYY.DDD.HHMM` at edit time), because changed content must
ship under a new version rather than reusing one already released.

### Why the fix belongs here rather than in the consumer

Advancing a consumer's pin instead does not help and measurably hurts — every option tried in
`distro-cachyos`, on one tree:

| Change in distro-cachyos | Warnings |
|---|---|
| baseline | **2** |
| advance `pod-selkies-core` alone | 6 |
| advance whole selkies family | 8 |
| advance ALL 23 stale pins (of 177) | 8 |

Advancing a consumer to a newer tag exposes that tag's *other* stale refs. The only operation
that removes an edge rather than trading it for others is fixing at the source, so the next
tag cut here carries current pins throughout.

Six such source-level fixes landed today — `layer-chrome-sway#2`, `layer-sway-desktop#3`,
`layer-xfce4-terminal#3`, `layer-sway-desktop-vnc#4`, `pod-charly-automation#4` and
`layer-sway-desktop#4` — each taking its own gate from exit 1 to exit 0 or removing a
measured conflict edge.

Context and the full measurement set: opencharly/distro-cachyos#48.

## How tested

```
$ charly box validate
exit 0
```

Run with charly **`v2026.241.1130`**, the version this repo's `deploy.yml` clones and builds,
so it is the same gate CI runs rather than a newer local binary. Exit code read directly from
`charly box validate`, not through a pipeline.

Sweep executed, output pasted rather than asserted — no old pin survives:

```
$ grep -c "v2026.235.2057\|v2026.239.1300" charly.yml
0
```

The three refs advanced: `layer-ffmpeg v2026.235.2057 -> v2026.239.1614`,
`layer-supervisord v2026.239.1300 -> v2026.240.0121`,
`pod-pipewire v2026.235.2057 -> v2026.239.1555`.

Diff is `4 insertions(+), 4 deletions(-)` in `charly.yml` — the 3 refs plus the version line,
nothing else.

The consuming project's warning drop lands with the repin that follows this tag: a stale ref
inside a published tag only stops resolving once a new tag exists, so no run in *this* repo
can demonstrate it.


**Correction to the previous version of this body:** it pasted a literal placeholder
(`grep -c "<the old pin versions>"`) instead of a real command and output. That is exactly
the described-but-unrun claim the rule exists to catch, and the validator was right to
block it. The command and output below were actually executed on this branch.

## Rulebook compliance

- **R0 (skills)** — `/charly-image:layer` for the candy/ref model;
  `/charly-internals:git-workflow` for PR-only landing and tag-on-merge.
- **R1 (RCA)** — the mechanism is identified precisely (published tags embed pins at tag
  time), and the consumer-side alternative was tested and measured to be worse, not assumed.
- **R2 (no deferral)** — a stale-pin set found while chasing a downstream warning is fixed in
  the repo that owns it, not labelled pre-existing.
- **R3 (no duplication)** — pins advanced in place; nothing restated locally.
- **R4 (no workarounds)** — pins are advanced rather than warnings allowlisted.
- **R5 (hard cutover)** — the pin values change with no old path left behind; sweep output
  pasted above.
- **R6 (git safety)** — feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** — `charly box validate` run on the committed tree with the
  repo's own pinned charly.
- **R8 (generation)** — N/A: no generated artifact, no CUE input in this repo.
- **R9 (artifact integrity)** — N/A: no image is built here.
- **ADE (candy description/plan)** — N/A: `description:` and `plan:` untouched.
- **SDD (CUE source)** — N/A: no schema authored or generated here.

## Change classification

- **Change class:** candy ref pin bumps (3 lines) + the candy's own CalVer bump. No packages,
  services, plan steps or scripts change.
- **Verification gate:** `charly box validate` under charly `v2026.241.1130`, exit 0
- **Attribution tier:** `analysed on a live system` — the gate was executed on a real checkout
  with the exit code recorded, and the skew class this removes was counted across real bed
  builds; no image is built in this repo

*Assisted-by: Claude Code Anthropic Opus 5 (analysed on a live system)*